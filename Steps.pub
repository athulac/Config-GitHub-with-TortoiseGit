use git bash cmd
+++++++++++++++++++++++++++++++
generate ssh rsa key : 
ssh-keygen -t rsa -b 4096 -C "email@gmail.com"

(can keep optional passphrase)
 
generate ssh key :
eval $(ssh-agent -s)

add ssh key :
ssh-add ~/.ssh/id_rsa

copy ssh key to clip board:
clip < ~/.ssh/id_rsa.pub

paste key on git hub by adding ssh key
